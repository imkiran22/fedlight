{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.21.0"
  },
  "homepage": "https://imkiran22.github.io/fedlight",
  "scripts": {
    "predeploy": "yarn lerna exec -- rimraf build dist && yarn lerna build",
    "deploy": "gh-pages -d ./packages/app-shell/build"
  },
  "workspaces": [
    "./packages/**"
  ],
  "dependencies": {
    "gh-pages": "^3.0.0"
  }
}
